class User {
  constructor(id, role, gender, name, imageURL) {
    this.id = id;
    this.role = role;
    this.gender = gender;
    this.name = name;
    this.imageURL = imageURL;
  }
}
export default User;
